# Demo Vault

This vault contains fake entries for local development and screenshots. It is safe to commit and can be copied into `server/tmp/demo-vault` by `server/scripts/run-demo-server.sh`.

The real diary vault stays in `vault/`, which is intentionally ignored by Git.
